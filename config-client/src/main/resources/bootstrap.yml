spring:
  cloud:
    config:
      name: config-client
      label: develop
      profile: dev
      discovery:
        enabled: true
        serviceId: config-server
eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
    serviceUrl:
      defaultZone: http://eurekamaster.com:8888/eureka